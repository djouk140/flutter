specVersion: "v1"
serviceId: "agrico"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "agrico-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
